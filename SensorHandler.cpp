#include "SensorHandler.h"

 void SensorHandler::echoCheck() { // If ping received, set the sensor distance to array.
	 /*if (sonar[currentSensor].check_timer()) {
		 cm[currentSensor] = sonar[currentSensor].ping_result / US_ROUNDTRIP_CM;
	 }*/
}